# support v0.1.14 to latest, almost all versions
# run command `nvm ls-remote` to list all version can be install

nodejs_version: "14"

# useful extra packages for Node, yarn global add
#noejs_packages_default: []
  #- bower
  
# useful extra packages for Node, yarn global add
#noejs_packages_extra: []
  #- gulp
  #- @vue/cli
  #- @angular/cli

nodejs_applications: []
  # - express
  # - vue

nodejs_applications_vue: 
  port:

nodejs_applications_express: 
  port: "3000"
